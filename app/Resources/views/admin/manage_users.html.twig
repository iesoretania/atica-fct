{% trans_default_domain 'admin' %}
{% extends 'layout.html.twig' %}
{% import '::macros.html.twig' as m %}
{% block content %}
    <h2>{{ 'user.title'|trans }}</h2>
    {% include ':admin:manage_users_partial.html.twig' with {'elements': pagination } %}

    {{ m.link_button(path('admin_menu'), 'arrow-left', 'btn-default', 'form.back'|trans) }}
    {{ m.link_button(path('admin_user_new'), 'plus', 'btn-success', 'user.new'|trans) }}
{% endblock %}
