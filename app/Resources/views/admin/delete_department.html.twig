{% trans_default_domain 'department' %}
{% extends 'layout.html.twig' %}
{% import '::macros.html.twig' as m %}

{% block content %}
    <div class="alert alert-warning">{{ 'prompt.delete_confirm'|trans|nl2br }}</div>
    <form method="post">
        {{ m.link_button(path('admin_department_form', {'department': department.id}), 'arrow-left', 'btn-default', 'form.delete_back'|trans) }}
        {{ m.submit_button('delete', 'trash', 'btn-danger', 'form.delete'|trans) }}
    </form>
{% endblock %}
