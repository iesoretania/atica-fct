{% trans_default_domain 'student' %}
{% extends 'layout.html.twig' %}
{% import '::macros.html.twig' as m %}

{% block content %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
    <h1>{{ 'work_tutor.title'|trans }}</h1>
    {{ form_start(form2) }}
        {{ form_widget(form2) }}
    {{ form_end(form2) }}
    {{ m.link_button(path(back, {'id':agreement.id}), 'arrow-left', 'btn-default', 'form.back'|trans) }}
{% endblock %}
