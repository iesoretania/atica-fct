{% extends 'layout.html.twig' %}
{% import '::macros.html.twig' as m %}

{% block content %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
    {{ m.link_button(path('frontpage'), 'arrow-left', 'btn-default', 'menu.back'|trans({}, 'menu')) }}
{% endblock %}
