{% trans_default_domain 'activity' %}
{% extends 'layout.html.twig' %}
{% import '::macros.html.twig' as m %}

{% block content %}
    <div class="alert alert-warning">{{ 'prompt.learning_outcome.delete_confirm'|trans|nl2br }}</div>
    <form method="post">
        {{ m.link_button(path('admin_program_learning_outcome_form', {id: element.id}), 'arrow-left', 'btn-default', 'form.delete_back'|trans) }}
        {{ m.submit_button('delete', 'trash', 'btn-danger', 'form.delete'|trans) }}
    </form>
{% endblock %}
