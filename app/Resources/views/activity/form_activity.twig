{% trans_default_domain 'activity' %}
{% extends 'layout.html.twig' %}
{% import '::macros.html.twig' as m %}

{% block content %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ m.start_button }}
        {{ m.submit_button('submit', 'check', 'btn-success', 'form.submit'|trans) }}
        {% if not new %}
            {{ m.link_button(path('admin_program_activity_delete', {'id': item.id}), 'trash', 'btn-danger', 'form.delete'|trans) }}
        {% endif %}
        {{ m.end_button }}
    {{ form_end(form) }}
    {{ m.link_button(path('admin_program_training', {'id': item.training.id}), 'arrow-left', 'btn-default', 'form.back'|trans) }}
{% endblock %}
